<div class="page-wrapper" ng-controller="CalendarController">
<!-- 
	<div class="col-sm-2">

		<div class="btn-group calTools">
			<button type="button" class="btn btn-primary" ng-click="addEvent()">
				Add Event
			</button>
				<button  type="button" class="btn " ng-click="changeView('agendaDay', myCalendar)">AgendaDay</button>
				<button type="button"  class="btn " ng-click="changeView('agendaWeek', myCalendar)">AgendaWeek</button>
				<button type="button" class="btn" ng-click="changeView('month', myCalendar)">Month</button>
		</div>

		<ul class="unstyled">
			<li ng-repeat="e in events">
				<div class="alert alert-info">
					<a class="close" ng-click="remove($index)"><i class="icon-remove"></i></a>
					<b> <input ng-model="e.title"></b> 
					{{e.start | date:"MMM dd"}} - {{e.end | date:"MMM dd"}}
				</div>
			</li>
		</ul>

	</div> -->
	<div class ="col-sm-12">
		<!-- <div class="alert-success calAlert" ng-show="alertMessage != undefined && alertMessage != ''">
			<h4>{{alertMessage}}</h4>
		</div> -->
		<div class="btn-toolbar">
	<div class="calendar" ng-model="eventSources" calendar="myCalendar" config="uiConfig.calendar" ui-calendar="uiConfig.calendar"></div>
	</div>
</div>
